Optional: drop trimmed MP4 clips here to replace the YouTube embeds.

Expected filenames (the site auto-detects them, no code changes needed):
  recruiter.mp4
  developer.mp4
  stalker.mp4

If a file is missing, that profile falls back to the YouTube embed
configured in js/data.js, and then to a still poster image.

Keep each clip ~30s, muted-friendly, and ideally under 8 MB so the
page stays fast and the repo stays small. Only upload footage you
have the right to redistribute.
